{% extends "base.html.twig" %}
{% block content %}
    <div class="content-background">
        <div class="content">
           <h1 class="h1-l"> {{ baan.name }} </h1>
            <p class="paragraph--l">{{ baan.description }}</p>
        </div>
    </div>
{% endblock %}
